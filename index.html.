<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<title>Nuestro Amor</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<style>

body{
    margin:0;
    background:linear-gradient(to bottom,#1b1b2f,#162447);
    font-family:'Segoe UI',sans-serif;
    overflow:hidden;
    display:flex;
    justify-content:center;
    align-items:center;
    height:100vh;
    color:white;
}

/* CONTENEDOR VERTICAL */
.card{
    width:360px;
    height:640px;
    background:rgba(255,255,255,0.05);
    backdrop-filter:blur(10px);
    border-radius:30px;
    padding:20px;
    position:relative;
    text-align:center;
    overflow:hidden;
}

/* TEXTO */
h2{
    font-weight:normal;
    margin-bottom:10px;
}

.signature{
    font-size:13px;
    opacity:0.7;
    margin-bottom:15px;
}

.counter{
    margin-top:15px;
    font-size:16px;
}

.time{
    font-weight:bold;
    margin-top:5px;
}

/* ÁRBOL */
.tree{
    position:relative;
    width:100%;
    height:330px;
    margin-top:10px;
}

.trunk{
    position:absolute;
    bottom:0;
    left:50%;
    transform:translateX(-50%);
    width:20px;
    height:120px;
    background:#8b4513;
    border-radius:10px;
}

.leaf{
    position:absolute;
    color:#ff4d6d;
    font-size:14px;
    animation: fadeIn 2s ease forwards;
    opacity:0;
}

@keyframes fadeIn{
    to{opacity:1;}
}

/* Corazones cayendo */
.falling{
    position:absolute;
    color:#ff4d6d;
    animation: fall linear infinite;
}

@keyframes fall{
    0%{transform:translateY(-10vh);}
    100%{transform:translateY(100vh);}
}

/* BOTÓN */
button{
    margin-top:10px;
    padding:8px 15px;
    border:none;
    border-radius:20px;
    background:#ff4d6d;
    color:white;
    cursor:pointer;
}

/* CARTA */
.letter{
    position:absolute;
    bottom:0;
    left:0;
    width:100%;
    height:100%;
    background:#fff0f3;
    color:#333;
    padding:30px 20px;
    border-radius:30px;
    transform:translateY(100%);
    transition:0.6s ease;
    display:flex;
    flex-direction:column;
    justify-content:flex-start;
    overflow-y:auto;
    box-sizing:border-box;
}

.letter.open{
    transform:translateY(0);
}

.letter h3{
    margin-top:10px;
}

.letter p{
    margin-top:15px;
    line-height:1.5;
}

</style>
</head>
<body>

<div class="card">

<h2>Para el amor de mi vida 💖</h2>
<div class="signature">Un regalo de David Franco a Carolina Vásquez</div>

<p>
Si pudiera elegir un lugar seguro, sería a tu lado.<br>
Cuanto más tiempo estoy contigo más te amo.
</p>

<div class="counter">
Mi amor por ti comenzó hace...
<div class="time" id="time"></div>
</div>

<div class="tree" id="tree">
<div class="trunk"></div>
</div>

<button onclick="openLetter()">Abrir carta 💌</button>

<!-- 🔥 NUEVO BOTÓN PARA IR A LA OTRA PÁGINA -->
<a href="arbol.html">
<button>Ver nuestro árbol especial 🌳💖</button>
</a>

<div class="letter" id="letter">
<h3>Mi amor ❤️</h3>
<p>
Desde que llegaste a mi vida todo cambió.  
Gracias por existir, por tu sonrisa y por cada momento juntos.  

Eres mi lugar seguro, mi paz y mi felicidad.  
No importa el tiempo que pase, cada día contigo es un regalo.  

Te amo más de lo que las palabras pueden explicar.
</p>

<button onclick="closeLetter()">Cerrar</button>
</div>

</div>

<audio autoplay loop>
<source src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3" type="audio/mpeg">
</audio>

<script>

const startDate = new Date("2022-04-30T00:00:00");

function updateCounter(){
const now = new Date();
const diff = now - startDate;

const s = Math.floor(diff/1000)%60;
const m = Math.floor(diff/1000/60)%60;
const h = Math.floor(diff/1000/60/60)%24;
const d = Math.floor(diff/1000/60/60/24);

document.getElementById("time").innerHTML =
d+" días "+h+" horas "+m+" minutos "+s+" segundos";
}

setInterval(updateCounter,1000);
updateCounter();

const tree = document.getElementById("tree");

function createHeartTree(){
for(let t=0;t<200;t+=0.2){
let heart=document.createElement("div");
heart.classList.add("leaf");
heart.innerHTML="❤";

let x = 16*Math.pow(Math.sin(t),3);
let y = 13*Math.cos(t)-5*Math.cos(2*t)-2*Math.cos(3*t)-Math.cos(4*t);

heart.style.left = (160 + x*7)+"px";
heart.style.top = (180 - y*7)+"px";
heart.style.animationDelay = (Math.random()*2)+"s";

tree.appendChild(heart);
}
}

createHeartTree();

function fallingHearts(){
let heart=document.createElement("div");
heart.classList.add("falling");
heart.innerHTML="❤";
heart.style.left=Math.random()*100+"vw";
heart.style.fontSize=(Math.random()*20+10)+"px";
heart.style.animationDuration=(Math.random()*3+3)+"s";
document.body.appendChild(heart);
setTimeout(()=>heart.remove(),6000);
}

setInterval(fallingHearts,300);

function openLetter(){
document.getElementById("letter").classList.add("open");
}

function closeLetter(){
document.getElementById("letter").classList.remove("open");
}

</script>

</body>
</html>
